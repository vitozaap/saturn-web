// <ViewTransition> lives in React's canary channel, which Next bundles for the
// App Router. The types ship separately, so pull them in project-wide.
/// <reference types="react/canary" />

export {};
